plugins {
    id 'nva.api.integration.testing.root-module-conventions'
}

group = 'no.sikt'
version = '1.0-SNAPSHOT'


tasks.withType(Test).configureEach {
    useJUnitPlatform()
    if (project.hasProperty('awsProfile')) {
        systemProperty 'aws.profile', project.awsProfile
    }
}
