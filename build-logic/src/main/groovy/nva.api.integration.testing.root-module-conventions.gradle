plugins {
    id 'nva.root-module-conventions'
}

nva {
    dependencyAnalysis {
        enforced = true
    }
    jacoco {
        minMethodCoverage = 0.800 // FIXME: Add test coverage and bump to 1
        minClassCoverage = 0.800 // FIXME: Add test coverage and bump to 1
    }
}
