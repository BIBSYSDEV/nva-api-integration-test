plugins {
    id 'nva.java-conventions'
    id 'java-test-fixtures'
}

nva {
    pmd {
        ignoreFailures = true // FIXME: Fix violations and enable this
    }
    spotless {
        enabled = false // FIXME: Reformat and enable this
    }
}
